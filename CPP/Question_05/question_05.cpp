/*
    Rewrite the code after correcting error(s).

    Difficuly Level : MEDIUM
*/

#include <iostream>

using namespace std;

int main() {
    struct lib {
        char bname[20];
        char author[20];
        float price, edition;
        date e;
    }l;
    struct date
    { intdd, mm, yy }
    
    char ch;
    price = 0;
    cout << "\nEnter Details : " cin >> e.l.mm >> e.l.yy >> cin >> e.l.dd;
    cin >> l.price; gets(l.author);
    if(l.e.dd <= 25 && l.e.mm <= 12)
    l.price += 20;
    else
    cout << "No Change";
    return 0;
}
