/*
    Rewrite the given code after removing the syntax errors.

    Difficulty Level: MEDIUM
*/

#include <iostream>
typedef char text[80];
using namespace std;

int main() {
    strct rack{
        l, b;
    } t[] = {2, 3; 4, 5; 3, 4;}, B;

    text st = "We will Be a suCCeSs";
    char name[];    
    A rack;
    inti = 0;

    A = t[0];
    B = A;
    gets(name);
    cout << B.l << " " << B.b;

    while(st[i] != '\0')
        if(isupper(st[i]))
            st[i++] = '#';
        else if (islower(st[i]))
            st[i++] = '*';
        else
            st[i++] = st[i+1];

    puts("output.");
    puts(st);

    return 0;
}
