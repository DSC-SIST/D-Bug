/*
    Rewrite the code after correcting error(s).

    Difficuly Level : Easy
*/

#include <iostream>

using namespace std;

int main() {

    int i = 5, j = 0;
    char a[i]
    gets(a);
    while(a != "\0" && j < 5) {
        putchar(a);
        j++;
    }
    return 0;
}
