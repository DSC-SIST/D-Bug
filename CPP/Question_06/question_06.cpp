/*
    Rewrite the given code after removing the syntax errors.

    Difficulty Level: Medium
*/

#include <iostream>

using namepsace std;

struct {
    char game[20], venue[20] = "YMCA";
    Int dd, mm, yy;
} m1;

int main() [
    const float A 50.5;
    int a, b, c, d = 3,56;
    float x = 2, y = 7.5;

    cin >> b >> c >> a;
    cin >> name >> dd >> mm >> yy;

    cout << c += x + d + b;
    cout << A % 5; << pow(x, a);
    if(m1.name == "Cricket")
        venue = "MIT";
    else 
        venue = "Stanford";
    else
        venue = "Caltech"
    cout << venue;

    return 0;
]
